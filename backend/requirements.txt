# Core web server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# ML stack
# NOTE: torch/torchvision are pinned deliberately, not arbitrarily —
# basicsr 1.4.2 imports torchvision.transforms.functional_tensor,
# which was REMOVED in torchvision 0.17+. Newer pins here will fail at
# import time with "ModuleNotFoundError: functional_tensor" the moment
# GFPGAN loads. If you need a newer torch for other reasons, see the
# patch note in README.md instead of bumping this blindly.
torch==2.1.2
torchvision==0.16.2
numpy==1.26.4
opencv-python-headless==4.10.0.84
pillow==10.3.0

# Face/image restoration
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
realesrgan==0.3.0
